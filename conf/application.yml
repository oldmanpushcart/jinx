# Micronaut 框架核心配置
micronaut:
  application:
    name: jinx
  server:
    # 服务器默认字符集
    charset: UTF-8
    # 服务监听端口
    port: 8080
    # 服务绑定地址 (0.0.0.0 表示监听所有网络接口)
    host: 127.0.0.1
    # 服务器空闲超时时间（针对入站请求，即别人调用你的接口）
    idle-timeout: 60s
    # 服务器连接建立超时时间
    connect-timeout: 5s
